import { RouteNames } from "../../../core/router/RouteNames";

export const routeVacancy = RouteNames.ADS;
export const routeAnonnsemend = `${RouteNames.ADS}/${RouteNames.ADS_LIST}`;
export const routeAccount = `${RouteNames.ADS}/${RouteNames.ADS_QUESTIONNAIRE_LIST}`;